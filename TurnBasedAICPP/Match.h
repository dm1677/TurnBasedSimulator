#pragma once
#include "Simulator.h"
#include <vector>

class Match
{
//public:
	//void UpdateState(const Action& action);
//private:
	//const Simulator m_simulator;
	//const std::vector<Action> m_actions;
};